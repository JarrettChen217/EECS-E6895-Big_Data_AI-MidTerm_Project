# No torch: install only the rest of deps in a venv where you already have PyTorch (e.g. cu130/cu124) to avoid overwriting
# Usage:  pip install -r requirements-no-torch.txt

# Core LLM (Hugging Face)
# Pin transformers<5 if sharing venv with chronos-forecasting (it requires transformers<5,>=4.41)
huggingface-hub>=1.3.0
transformers>=4.36.0,<5
accelerate

# RAG (LangChain + vector store)
langchain>=0.2.0
langchain-community>=0.2.0
langchain-core>=0.2.0
langchain-text-splitters>=0.2.0
langchain-huggingface
faiss-cpu
sentence-transformers

# Optional: OpenAI backend
openai>=1.0.0

# API
flask>=3.0.0

# Config
python-dotenv>=1.0.0

# Utilities
numpy
pandas
