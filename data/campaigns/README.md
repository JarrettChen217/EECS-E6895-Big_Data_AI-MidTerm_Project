# Campaign creative assets

Place ad creative images here. The healthcare campaign expects:

- **healthcare_sample.jpg** — used when the user asks for a "healthcare" ad solution (see `data/campaigns.json`).

Add more campaigns by editing `data/campaigns.json` and placing the corresponding image files in this directory.
